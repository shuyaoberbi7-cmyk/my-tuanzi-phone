<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>🍡 團子小手機</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
        }

        body {
            background: #f0e6f6;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 16px;
        }

        /* 手機外框 */
        .phone {
            width: 100%;
            max-width: 390px;
            height: 780px;
            background: #fcf8ff;
            border-radius: 44px;
            box-shadow: 0 25px 60px rgba(180, 130, 200, 0.25), inset 0 0 0 2px rgba(255, 255, 255, 0.6);
            overflow: hidden;
            position: relative;
            display: flex;
            flex-direction: column;
            transition: all 0.3s ease;
        }

        /* 動態島 */
        .dynamic-island {
            position: absolute;
            top: 12px;
            left: 50%;
            transform: translateX(-50%);
            width: 120px;
            height: 34px;
            background: #1c1c1e;
            border-radius: 20px;
            z-index: 50;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 16px;
        }

        .dynamic-island .camera-dot {
            width: 10px;
            height: 10px;
            background: #2a2a2e;
            border-radius: 50%;
            border: 1px solid #3a3a3e;
        }

        .dynamic-island .sensor-dot {
            width: 40px;
            height: 6px;
            background: #1a1a1e;
            border-radius: 4px;
            opacity: 0.6;
        }

        /* 鎖屏畫面 */
        .lock-screen {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(145deg, #f9e8ff 0%, #f0d6ff 100%);
            z-index: 40;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 60px 30px 80px;
            transition: transform 0.5s cubic-bezier(0.32, 0.72, 0, 1);
            transform: translateY(0);
        }

        .lock-screen.unlocked {
            transform: translateY(-100%);
            pointer-events: none;
        }

        .lock-screen .time-display {
            font-size: 72px;
            font-weight: 300;
            color: #3d2a4a;
            letter-spacing: 2px;
            margin-top: 60px;
            font-variant-numeric: tabular-nums;
        }

        .lock-screen .date-display {
            font-size: 18px;
            color: #6b4f7a;
            margin-top: 6px;
            font-weight: 400;
            letter-spacing: 1px;
        }

        .lock-screen .lock-icon {
            font-size: 28px;
            margin-top: 16px;
            opacity: 0.7;
            color: #7a5a8a;
        }

        .lock-screen .password-hint {
            font-size: 14px;
            color: #8a6a9a;
            margin-top: 8px;
            opacity: 0.8;
            background: rgba(255, 255, 255, 0.4);
            padding: 6px 18px;
            border-radius: 30px;
            backdrop-filter: blur(4px);
        }

        /* 滑動解鎖 */
        .slide-to-unlock {
            position: absolute;
            bottom: 50px;
            left: 30px;
            right: 30px;
            height: 64px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 40px;
            backdrop-filter: blur(8px);
            border: 1px solid rgba(255, 255, 255, 0.5);
            display: flex;
            align-items: center;
            padding: 4px;
            cursor: pointer;
            transition: all 0.2s;
            user-select: none;
            touch-action: none;
        }

        .slide-to-unlock .slider-thumb {
            width: 56px;
            height: 56px;
            background: linear-gradient(135deg, #e8c4f0, #d4a8e0);
            border-radius: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            co lor: #7a5a8a;
        }

        .lock-screen .password-hint {
            font-size: 14px;
            color: #8a6a9a;
            margin-top: 8px;
            opacity: 0.8;
            background: rgba(255, 255, 255, 0.4);
            padding: 6px 18px;
            border-radius: 30px;
            backdrop-filter: blur(4px);
        }

        /* 滑動解鎖 */
        .slide-to-unlock {
            position: absolute;
            bottom: 50px;
            left: 30px;
            right: 30px;
            height: 64px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 40px;
            backdrop-filter: blur(8px);
            border: 1px solid rgba(255, 255, 255, 0.5);
            display: flex;
            align-items: center;
            padding: 4px;
            cursor: pointer;
            transition: all 0.2s;
            user-select: none;
            touch-action: none;
        }

        .slide-to-unlock .slider-thumb {
            width: 56px;
            height: 56px;
            background: linear-gradient(135deg, #e8c4f0, #d4a8e0);
            border-radius: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            color: #fff;
            box-shadow: 0 4px 12px rgba(160, 100, 180, 0.3);
            transition: all 0.1s;
            flex-shrink: 0;
        }

        .slide-to-unlock .slider-text {
            flex: 1;
            text-align: center;
            font-size: 17px;
            font-weight: 500;
            color: #5a3a6a;
            letter-spacing: 2px;
            opacity: 0.7;
        }

        .slide-to-unlock.dragging .slider-thumb {
            transform: scale(0.95);
        }

        .slide-to-unlock.unlocked .slider-thumb {
            background: #b8e0b8;
        }

        /* Face ID 模擬按鈕 */
        .face-id-btn {
            position: absolute;
            bottom: 130px;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(8px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 10px 28px;
            border-radius: 40px;
            font-size: 15px;
            color: #5a3a6a;
            cursor: pointer;
            transition: all 0.2s;
            font-weight: 500;
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .face-id-btn:hover {
            background: rgba(255, 255, 255, 0.4);
        }

        .face-id-btn:active {
            transform: translateX(-50%) scale(0.95);
        }

        /* 密碼輸入 */
        .password-input-area {
            position: absolute;
            bottom: 200px;
            left: 0;
            right: 0;
            display: none;
            justify-content: center;
            gap: 12px;
            padding: 0 30px;
        }

        .password-input-area.active {
            display: flex;
        }

        .password-dot {
            width: 16px;
            height: 16px;
            border-radius: 50%;
            background: rgba(60, 40, 70, 0.2);
            border: 2px solid rgba(60, 40, 70, 0.2);
            transition: all 0.2s;
        }

        .password-dot.filled {
            background: #7a5a8a;
            border-color: #7a5a8a;
        }

        /* 主畫面 */
        .home-screen {
            flex: 1;
            background: #fcf8ff;
            display: flex;
            flex-direction: column;
            position: relative;
            padding-top: 50px;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.4s ease;
        }

        .home-screen.active {
            opacity: 1;
            pointer-events: auto;
        }

        /* 狀態列 */
        .status-bar {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 50px;
            padding: 14px 24px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 10;
            font-size: 14px;
            font-weight: 600;
            color: #3d2a4a;
        }

        .status-bar .left {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .status-bar .left .time {
            font-size: 15px;
            font-weight: 600;
            letter-spacing: 0. 5px;
        }

        .status-bar .left .cat-paw {
            font-size: 16px;
            filter: drop-shadow(0 1px 2px rgba(0,0,0,0.05));
        }

        .status-bar .right {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 14px;
            color: #4d3a5a;
        }

        .status-bar .right .wifi-icon,
        .status-bar .right .battery-icon {
            font-size: 16px;
            opacity: 0.8;
        }

        /* 主畫面 App 網格 */
        .app-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 16px 8px;
            padding: 16px 16px 0;
            margin-top: 8px;
            flex: 1;
            align-content: start;
        }

        .app-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 4px;
            cursor: pointer;
            transition: all 0.15s;
            padding: 8px 0;
            border-radius: 20px;
        }

        .app-item:active {
            transform: scale(0.88);
            background: rgba(180, 130, 200, 0.08);
        }

        .app-item .app-icon {
            width: 64px;
            height: 64px;
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            background: linear-gradient(145deg, #f0e4f8, #e8d6f0);
            box-shadow: 0 4px 12px rgba(160, 120, 180, 0.15);
            transition: all 0.2s;
        }

        .app-item .app-name {
            font-size: 11px;
            color: #4d3a5a;
            font-weight: 500;
            text-align: center;
            letter-spacing: 0.3px;
            max-width: 64px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* Dock 欄 */
        .dock {
            background: rgba(240, 230, 248, 0.6);
            backdrop-filter: blur(20px);
            border-radius: 30px;
            margin: 8px 16px 16px;
            padding: 10px 12px;
            display: flex;
            justify-content: space-around;
            border: 1px solid rgba(255, 255, 255, 0.4);
            flex-shrink: 0;
        }

        .dock .app-item {
            flex: 1;
            padding: 4px 0;
        }

        .dock .app-item .app-icon {
            width: 56px;
            height: 56px;
            font-size: 28px;
            background: linear-gradient(145deg, #f5ecfa, #ece0f2);
        }

        .dock .app-item .app-name {
            font-size: 10px;
        }

        /* App 視窗 (彈窗) */
        .app-window {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #fcf8ff;
            z-index: 60;
            border-radius: 44px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            transform: scale(0.92) translateY(20px);
            opacity: 0;
            pointer-events: none;
            transition: all 0.3s cubic-bezier(0.32, 0.72, 0, 1);
        }

        .app-window.open {
            transform: scale(1) translateY(0);
            opacity: 1;
            pointer-events: auto;
        }

        .app-window .app-header {
            padding: 16px 20px 12px;
            background: rgba(248, 240, 252, 0.8);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(200, 170, 210, 0.2);
            display: flex;
            align-items: center;
            gap: 12px;
            flex-shrink: 0;
            min-height: 56px;
        }

        .app-window .app-header .back-btn {
            font-size: 24px;
            cursor: pointer;
            background: none;
            border: none;
            padding: 4px 8px;
            border-radius: 30px;
            transition: background 0.15s;
            color: #5a3a6a;
        }

        .app-window .app-header .back-btn:active {
            background: rgba(160, 120, 180, 0.15);
        }

        .app-window .app-header .app-title {
            font-size: 18px;
            font-weight: 600;
            color: #3d2a4a;
            flex: 1;
        }

        .app-window .app-body {
            flex: 1;
            overflow-y: auto;
            padding: 16px 20px;
            -webkit-overflow-scrolling: touch;
        }

        /* 通用元件 */
        .chat-bubb le {
            max-width: 80%;
            padding: 12px 16px;
            border-radius: 20px;
            margin-bottom: 8px;
            word-wrap: break-word;
            line-height: 1.5;
            font-size: 15px;
        }

        .chat-bubble.user {
            align-self: flex-end;
            background: linear-gradient(135deg, #e8d6f0, #dcc4e8);
            color: #2d1a3a;
            border-bottom-right-radius: 4px;
        }

        .chat-bubble.char {
            align-self: flex-start;
            background: #f5ecfa;
            color: #2d1a3a;
            border-bottom-left-radius: 4px;
            border: 1px solid rgba(200, 170, 210, 0.2);
        }

        .chat-bubble .sender {
            font-size: 12px;
            font-weight: 600;
            color: #7a5a8a;
            margin-bottom: 4px;
        }

        .input-bar {
            display: flex;
            gap: 8px;
            padding: 8px 0;
            border-top: 1px solid rgba(200, 170, 210, 0.15);
            background: rgba(248, 240, 252, 0.5);
            backdrop-filter: blur(8px);
            flex-shrink: 0;
        }

        .input-bar input {
            flex: 1;
            border: none;
            background: rgba(240, 230, 248, 0.4);
            padding: 10px 16px;
            border-radius: 30px;
            font-size: 15px;
            outline: none;
            color: #2d1a3a;
        }

        .input-bar input::placeholder {
            color: #9a8aaa;
        }

        .input-bar button {
            background: linear-gradient(135deg, #dcc4e8, #c8b0d8);
            border: none;
            padding: 10px 20px;
            border-radius: 30px;
            font-size: 15px;
            color: #fff;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.15s;
        }

        .input-bar button:active {
            transform: scale(0.92);
        }

        /* 捲軸美化 */
        .app-body::-webkit-scrollbar {
            width: 4px;
        }

        .app-body::-webkit-scrollbar-track {
            background: transparent;
        }

        .app-body::-webkit-scrollbar-thumb {
            background: #d4c0dc;
            border-radius: 10px;
        }

        /* 響應式調整 */
        @media (max-width: 420px) {
            .phone {
                height: 100vh;
                max-height: 820px;
                border-radius: 32px;
            }
            .app-window {
                border-radius: 32px;
            }
            .lock-screen .time-display {
                font-size: 56px;
            }
        }

        @media (min-width: 600px) {
            .phone {
                height: 780px;
            }
        }
    </style>
</head>
<body>
```